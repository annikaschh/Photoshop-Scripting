// make a script that stores inlay color name & exports with name formatting

var thisLayer = activeDocument.layerSets[0].artLayers[0];

function storeCerakoteNames(){
    alert("Select first cerakote layer");
    var nameList = [thisLayer.name];
    for (int i = 0; i<51; i++){

    }
}
